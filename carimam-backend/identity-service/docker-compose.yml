version: '3.8'
services:
  carimam_db:
    image: postgres:15-alpine
    container_name: carimam_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pa5svword321carr1mann
      POSTGRES_DB: carimam_identity
    ports:
      - "8432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: